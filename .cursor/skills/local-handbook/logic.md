# Logic (Cursor supplement)

**Primary instructions:** follow **`AGENTS.md`** in the repository root.

Use this file only when:

- You are running inside **Cursor** and the host loaded this skill automatically; still **prefer `AGENTS.md`** for verification commands and workflow.
- You need a reminder that **Cursor-specific** configuration may live under `.cursor/` but **must not** replace or contradict **`AGENTS.md`**.

If anything here conflicts with **`AGENTS.md`**, **`AGENTS.md` wins**.
